import { LoggedInStyleDirective } from './logged-in-style.directive';

describe('LoggedInStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggedInStyleDirective();
    expect(directive).toBeTruthy();
  });
});
